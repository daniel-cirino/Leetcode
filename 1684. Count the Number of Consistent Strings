class Solution:
    def countConsistentStrings(self, allowed: str, words: List[str]) -> int:
        alla = set(allowed)
        count = 0
        
        for word in words:
            for letter in word:
                if letter not in alla:
                    count += 1 # count all the letters not in allowed
                    break
        
        return len(words) - count
